# AGENTS.md

## 项目状态

实施中（已发布 v0.0.1–v0.3.11）。`DESIGN.md` 是本插件的权威设计规范，所有实现必须遵守其中的锁定决策。仓库已有可构建代码：`internal/{config, service, store, money, usageparse, fx, httpapi, web}` 与入口 `main.go`（内联 C ABI）。请求路径实时额度强制（model_router/executor/request_interceptor/request_lifecycle_plugin）与被动统计模式均已在 `main.go` + `internal/service/requestpath.go` 实现；每次发布前需跑通全量回归（`go test ./...`、`scripts/smoke.go`、`scripts/abi-smoke.c`、`scripts/devserver.go`）。v0.0.1 Windows 构建失败原因：egor-tensin/setup-mingw 依赖的 chocolatey mingw 包安装脚本故障（libpthread.dll.a 缺失），已改为 CI 直接下载 winlibs 便携 MinGW（16.1.0posix-14.0.0-ucrt-r1）。v0.0.3 发布资产格式与宿主 pluginstore 不符（宿主要求 `<id>_<version>_<goos>_<goarch>.zip` + `checksums.txt`），v0.0.4 已按此规范重写打包。v0.1.0 管理面板完全重写为「仪表盘」风格三文件结构（console.html/css/js，构建期注入占位符合成单文件壳），界面语言应需求收敛为仅简体中文（取消四语言 i18n）；httpapi 的 /requests /trends /costs 经 parseFilter 统一支持 from/to/result 等过滤。v0.1.1：移除面板审计页；models.dev 计价改为搜索引入（10 分钟缓存）替代整本同步；宿主 usage.handle 作为权威用量口径回填零用量记录，TPS 缺失时自算；趋势图重做为堆叠柱状图（像素级尺寸 + 过密降采样）；延迟统一秒显示并拆首字/总延迟；顶栏工具组靠左避开宿主按钮区；深色模式跟随宿主主题。v0.1.2：新增计价清空（/pricing/reset，保留 glob:* 兜底）；修复宿主路由声明表漏 /pricing/search 导致面板搜索 404；趋势 tooltip 改顶部下挂修复不可见；柱状图间距收紧。v0.2.0：修复 quota 模式重复统计（usage.handle 的 APIKey 字段在部分兼容渠道是上游凭据，改为按 时间±15s+延迟±150ms+模型候选 关联执行器记录判重，命中仅回填缺失 token/首字延迟）；新增 store.RedactSource 清洗疑似上游凭据的 source 字段（写入+读取双端）；概览第四卡改缓存命中率（兼容 OpenAI cached_tokens 与 Claude cache_read 口径）；模型占比/密钥消耗支持 费用|Token|请求 指标切换；移除面板主题手动切换按钮（保留自动跟随宿主）。v0.2.1：修复双写竞态导致的重复统计——执行器结算与宿主 usage.handle 几乎同时落库，先查后插判重存在窗口；改为落库后对账（ReconcileRequestDuplicates：key_id 空否相反+模型候选+延迟±150ms+时间±15s 关联），保留执行器行、合并被动行零值字段并删除之，分钟聚合同步扣减/回补。v0.2.2：重复统计改为进程内「宿主用量认领」根治——宿主 usage.handle 不带请求 ID，任何库内启发式判重都会漏判/误判；执行器与拦截器在调用宿主上游前登记认领（归一化裸模型名匹配，同 kid 优先、否则 FIFO），命中的宿主回调交给该请求消费而不入库，库内判重（DedupeRequests/ReconcileRequestDuplicates）降级为跨进程与迟到回调的兜底；`host_usage_wait` 只用于流式兜底且改为「先关客户端流再等」（非流式不等待，宿主是在执行器返回之后才记账的）；宿主展示字段只在落库前合并（provider/source/auth_type/tier 属 usage_rollups 主键），落库后仅回填 token 与首字延迟；新增 `POST /dedupe` 与面板「对账去重」按钮，Maintain 每次自动按保留期对账；面板存储错误提示按真实成因分支（不再固定追加杀毒/同步盘那一句），系统页新增「存储重试」读数（`/health` 的 io_retries）；httpapi 路径改由 `route()` 登记并经 `Paths()` 与 `managementRegistration()` 交叉校验（拦住 v0.1.2 那类漏声明 404）；abi-smoke 不再卸载动态库（Go 运行时不支持卸载 c-shared，FreeLibrary 后偶发退出段错误，v0.2.1 及更早同样存在）。v0.2.3：被动入库（usage.handle 兜底路径）模型名改用宿主上报的别名 Alias 优先，修复 OpenRouter 等渠道把 openrouter/… 回报为 stealth/… 导致同一模型在维度统计里割裂成两个值；面板 Token 全部改用 K/M/B 国际单位自动升级（fmtTok，升级阈值取 999.5 倍数避免出现 1000K），概览「总 Token」改「总消耗 Token」并完整显示精确到个位；请求明细弹窗密钥显示标签而非 kid（keyLabelOf 共享辅助）；模型占比/密钥消耗条目间距加大（bar-cell 相邻 margin-top）。v0.3.0：修复在途预占永久泄漏——崩溃/重启残留的 held 预占没有任何清理调用方（ReleaseStale 是死代码），且 Reserve 的并发与额度统计会把它们计入，长期占用 Key 的额度余量与并发名额；改为 Reserve 事务内联清扫（HoldReservationParams.SweepStaleBefore，先扫后查）+ Maintain 附带释放，无后台定时器、零空闲负载。内存优化：删除 runStream 中只写不读的整流缓冲（每请求最多 max_buffer_bytes=4MB，用量解析已逐块增量进行），imageHolds 在 request.complete 丢失时连同心跳 goroutine 永久泄漏、改为登记时按 1h 清扫；max_buffer_bytes 配置彻底移除（非严格 YAML 解析下旧配置静默忽略）。缓存读展示统一两种上游口径：OpenAI/Gemini 的 cached_tokens（含于输入）与 Claude 的 cache_read_tokens 取较大者（cacheReadOf），应用到趋势图缓存读系列/请求表列/详情弹窗/概览命中率卡。维度聚合修复：占比分母改全量最大请求数（原取首行，费用并列的不稳定排序下出现 >100%）、费用并列时按请求数降序稳定排序；自算 TPS 加 3000 token/s 可信上限——宿主把整段响应缓冲后几毫秒内转发会算出物理不可能的 TPS 污染聚合均值，超限不落库（面板显示 "-"），历史脏数据随保留期老化。v0.3.1：请求明细搜索修复——后端模型筛选原为精确等值（部分名称永远查不到），改为「精确名或渠道/后缀」匹配（LIKE 已转义，与库内判重同口径）；模型/密钥筛选框改原生 datalist 联想下拉（候选来自维度接口与密钥列表，保留手动输入）；请求表密钥列显示标签回退 kid（悬浮 title 见完整 kid）；models.dev 搜索结果面板加标题栏与关闭按钮。TPS 展示层防线：前端 fmtTPS 与后端落库上限同口径（3000 token/s），请求表/维度聚合/详情弹窗三处超限一律显示 "-"，v0.3.0 之前入库的历史坏值不再露出（随保留期老化清除）。v0.2.4：修复流式请求 token 统计全零——宿主对插件发起的嵌套执行不上报用量（内层 InternalSource 不建 reporter、外层被嵌套标记抑制），且 openai→openai 直通翻译会剥掉 SSE 的 data: 前缀，stream_read 交付裸 JSON，而 Accumulator.FeedSSE 只认 data: 行；新增 FeedChunk（整段 JSON/SSE 自动识别 + 逐行重试兜底）替代 runStream 的喂入点。计价规则支持编辑：UpsertPricingRule 带 ID 时原位更新（键未变保 ID 与创建时间）、键变更迁移（新键已存在则合并）、ID 缺失报 ErrNotFound；面板每条规则加「编辑」按钮与预填弹窗，口径字段（accounting_mode/billing_mode/per_image/source）编辑时保留原值。v0.3.2（UI 优化，无后端改动）：本轮全部结论来自本地实测（新增 `scripts/seed.go` 拟真种子 —— 8 密钥/15 计价规则/3476 请求/13 模型/30 天含 1 天空缺；配色用 OKLab ΔE + OKLCH + Machado CVD 模拟 + WCAG 计算校验，不靠肉眼）。**图表配色**：趋势图四系列原直接复用品牌语义色，实测浅色下 `--signal` 彩度 0.094 低于 0.10 下限（发灰）、相邻 CVD ΔE 6.8 只在警告带，深色下四色明度 L=0.71–0.80 全部超出深色带 0.48–0.67（过亮发白）；改为新增**图表专用** `--series-1..4`（浅 #2a78d6/#eb6834/#1baf7a/#eda100，深 #3987e5/#d95926/#199e70/#c98500），两模式 5 项检查全过（相邻 CVD ΔE 9.1 浅 / 8.4 深，常视觉 22.9 / 19.8）；品牌语义色 `--signal/--trace/--live/--warn/--alarm` 原值保留，继续用于按钮/徽标/仪表/状态，「成功/失败」仍用状态色（good/bad 语义）。浅色下 series-3/4 低于 3:1 属**已豁免的对比条款**，成立前提是已提供替代读取途径（常在图例 + 表格孪生 + 键盘逐桶读数）——不要为拉高对比重新取步，那会破坏已跑通的 CVD 门槛，改任何值都要重跑校验器。**图表几何**：柱宽上限 44→24px；堆叠段间开 2px 表面色间隙（间隙统一开在每段顶边，最底段仍坐基线上）；栈顶段 4px 圆角、基线端方角（原 `rx=1` 四角全圆）；非零段至少 1px（原出现 0.1px 亚像素段）；tooltip 由固定 `top:10px` 改为上挂柱顶并按空间上下翻转。**可访问性**（均为实测确认的功能缺陷）：6 个 tabpanel 的 `aria-labelledby` 全部悬空（无 tab 带 id）已补齐；tablist 补 roving tabindex 与 ←/→/Home/End；时间范围弹层原先 Escape 关不掉，现统一 Escape 关浮层并归还焦点；图表数值原先只能鼠标悬停读取，现热区 `tabindex=0` + `aria-label`，键盘聚焦出 tooltip、←/→ 跨桶，并新增**表格孪生视图**（图表/表格切换）。**下拉框**：6 个原生 select + 2 个 datalist 全部换为自建 listbox/组合框（`.sel`/`.combo`，复用 .pop 视觉，含 ↑↓/Home/End/Enter/Esc/首字母跳转）——原生 select 弹出列表无法跨浏览器统一样式，datalist 在 Firefox 只显示 value 不显示 label（密钥筛选会只见 kid 不见标签）；**弹层必须挂 body + `position:fixed`**，实测 `.panel{overflow:hidden}` 会裁掉面板内绝对定位弹层（短面板下 260px 弹层被裁 120px）；弹窗 `<dialog>` 内的 select 保持原生（行为正常、无跨浏览器诉求）。注意 `f-caller` 原先克隆 `key-caller` 的 innerHTML，改组件后已改为走 `callersCache` 数据源重建。**请求明细表**：13 列共 1322px vs 容器 1184px（1280 视口）横向必滚 + 外层 max-height 竖向滚 = 一个卡片两个方向拖；改为「输入/输出/缓存读」与「首字/总延迟」合并为复合单元格 + 列偏好弹层（低频列按需开启，偏好存 localStorage），默认 8 列在 1280/1440/1600 三档实测均无横向滚动；模型列加 `max-width:340px` 截断（长名如 openrouter/… 会重新推出容器）。分页补每页条数（20/50/100）与跳页输入（原 75 页只有上/下一页）。**排序键错位修复**：旧版「首字」表头标 `data-sort="latency"`，而后端 latency 映射到 `latency_ms`（总延迟），点「首字」实际按总延迟排；合并后的「延迟 首字→总」排序键指向 latency 与表头一致，其余后端白名单（ts/cost/tokens/latency/model）之外的数值列一律不给排序 affordance。**其他**：`.bar-cell{min-width:150px}` 在 375px 视口的 minmax(0,1fr) 轨道里把占比条撑到 right:419>375 再被 panel 裁掉，改 min-width:0 由 ellipsis 承担收缩（移动端溢出元素 3→0，余 1 个是有意横滚的 tabs 条）；占比卡右侧原显示数值而类名叫 `.bar-pct`（维度表同名类显示真实百分比），现统一为「百分比 + 数值」；密钥表长标签补 `title`（截断后可恢复，请求表原本就有）；死 CSS 清理（`.readout-switch`/`.spin`/`.skeleton`/`.unit`/`.btn.select` 及孤立 keyframes，已脚本审计至 0 未引用类）。v0.3.3：**修复维度聚合「占比」不是占比** —— 分母取的是「最大行请求数」（v0.3.0 为修 >100% 引入），算出来是「相对最大值的比例」：最大行恒显示 100%，各行相加远超 100%（实测 result 维度 ok 100% + error 8% = 108%，provider 维度累计 263%，auth_type 181%），与列名和常识都不符。改为分母取所有分组请求数之和，八个维度实测合计均落在 98–102%（仅整数取整误差），无恒 100% 行；条长仍按最大值归一以保持可比。概览「模型占比/密钥消耗」同类缺陷一并修正：原分母取 `rows` 之和，但 `rows` 已被 slice 成前 10/前 8，尾部量被摊到头部导致份额系统性夸大且头部恒合计 100%；改为传入截断前的全量分母（实测前 10 项合计 92.4%，与其实际覆盖的 92.74% 一致），副标题补「取前 N（共 M 项）」让截断可见。注意 `GroupByDimension` 的 `total` 是在扫描循环内 accumulate 的，带 limit 时只累加返回行、**不是**全量总数，不能拿它当全量分母（本次已据此改用行内之和，并把该约束写进注释）。顺带：密钥维度分组值是 kid，改为显示标签、kid 移入 title（与请求表/概览/详情弹窗同口径），无标签时回退 kid。v0.3.4：**新增 Token 限额**（总量/日/周/月，与金额限额并列生效，任一触顶即拒绝）。动机：混合模型下价差可达数十倍（haiku vs opus），只用金额无法精确约束用量。口径为**计费四类合计**（输入＋输出＋缓存读＋缓存写，即 `usageparse.Billable().Sum()`），与费用同一口径 —— inclusive/exclusive 已归一、cached 不重复计、推理已并入输出。判定时机与金额完全对称：**预占期**按估算 token 就拦（复用早已存在但只记录不检查的 `reservations.reserved_tokens`），结算时按真实 token 回写累计器；上游未回用量时退回预占估算值，与 cost 走 HeldMicroUSD 的兜底逻辑一致。schema 升至 **v2**：plugin_keys 加 4 个上限列（NULL=不限）与 4 个累计器列；token 累计器**复用金额那套 `*_cycle_key`** 归零机制并在同一条 UPDATE 内推进，保证两种口径的跨期点严格一致。仅当 Key 配了 token 限额时才付那次额外查询的代价。实测：8 项 store 测试锁住 预占期拦截/在途预占计入/结算记真实值而非估算/跨周期归零/与金额相互独立/未配即不限；真实 v1 库迁移验证 8 键 16 规则 3476 请求全部完好、token 列默认 NULL。

**计价规则精简**：删除 `price_reasoning` 与 `price_cached` 两档 —— 查证 `costForRule` 只用 输入/输出/缓存读/缓存写 四档，推理 token 由 `Billable()` 并入输出按输出价计、cached 并入缓存读，这两个字段填了**永远不生效**，让用户填是误导。schema v2 一并 DROP COLUMN（已验证 modernc/sqlite 3.53.3 支持）。顺带修好一处真实计价偏差：models.dev 的 `input_cached` 原先落进从不参与计算的 `price_cached` 等于被丢弃，OpenAI 系模型的缓存读会按输入价高估；现按 cache_read → input_cached → 输入价 的顺序取值。

**术语说明（ⓘ）**：「缓存写/缓存读」这类术语中文里不自明，背后是上游计费口径差异。新增 `infoTip()` 与集中的 `TIPS` 表，在计价表头、计价表单、请求表头、密钥表单加 ⓘ 徽标（原生 title 承载，零依赖、`tabindex=0` 键盘可达、移动端长按可见）。密钥表单按「金额限额 / Token 限额」分组（`.form-sep`），否则 8 个「日/周/月限额」框排在一起分不清口径；编辑表单的占位符显示当前值。

**顺带修掉两个既有缺陷**（都是本次做 token 限额时实测撞出来的，与新功能无关）：（1）**金额限额编辑从未成功过** —— `parseKeyUpdate` 用 `money.ParseUSD(jsonStr(v))` 把 `*_micro_usd` 当美元字符串解析，而签发路径与面板都发裸整数 micro-USD，`jsonStr` 对 JSON 数字返回空串 → 恒报「金额格式非法」400。改为新增 `jsonInt64()` 按数字优先解析（兼容带引号写法），两条路径同口径。（2）**详情页金额表盘恒显示「余 $0 / 100% alarm」** —— 面板读 `b.total`/`b.daily`/`b.held`，而接口返回的是 `total_remaining_micro_usd` 等，全部 undefined 被 `?? 0` 静默当成 0；改用正确字段名，并让 `remainMeter` 在余量缺失时显示「—」而非伪装成额度耗尽。新增 5 项 httpapi 测试把这两条钉住（含 balance 的 JSON 字段名契约）。v0.3.5：**密钥管理界面重写为卡片列表**（纯前端，功能零变更）——原 10 列表格把 kid 串、双额度仪表与四个数字列挤在一行互相踩；改为纵向通栏卡片，三段层级：状态胶囊+标签+最后使用+展开箭头的头行、kid·caller·已用·今日·并发的元信息行、金额/Token 两块宽幅仪表并排作为视觉主角。详情展开由 `tr.detail` 改为 `.key-detail` 兄弟节点（facts 网格/余额仪表组/操作按钮原样），卡片支持键盘 Enter/空格展开与焦点环。移除表格专用死规则（cell-key/w-meter/w-chev/tr.detail）。v0.3.6：密钥页二次改版——卡片改**多列自适应网格**（`repeat(auto-fill,minmax(285px,1fr))`，窄屏降单列），原生 `<button>` 承载键盘与悬停态；详情/余额从行内展开改为点击卡片弹 **dialog**（复用 sheet：状态头+kid+facts+余额仪表组+操作按钮行，余额异步核算、关窗后不回写）；搜索/caller/状态筛选合并为面板头下的一排控制栏 `.keys-toolbar`（签发按钮移至标题行右）。v0.3.7：卡片金额/Token 双仪表改卡内两列并排（最小列宽提至 320px），时间加「最近使用: 」前缀；dialog 余额区重写为对齐的三列配额清单 `balRow`（名称 | 单条进度 | 余 X / 上限，warn/alarm 状态色沿用语义令牌），移除散装的 remainMeter/remainTokMeter/meter-group/meter-grid 死代码。v0.3.8：密钥界面**结构性重写为主从双栏**——左栏紧凑清单（状态圆点+标签+迷你额度读数，分页随栏），右栏常驻详情（标签/状态/最近使用头部、kid·caller·并发行、facts、balRow 余额配额清单、操作按钮），选中驱动替代卡片+弹窗；筛选/翻页/删除后自动选中第一条可见项，右栏永不为空；`balanceSeq` 竞态守卫防快速切换串数据；键盘 Enter/空格可选；<860px 双栏纵向堆叠；清理 keyDetailSheet/key-card 全套/meterHTML/tokMeterHTML 死代码。v0.3.9：密钥界面**第三次重构为画廊卡片 + 右侧滑出抽屉**——卡片网格（`repeat(auto-fill,minmax(280px,1fr))`）承载状态胶囊+最近使用、可复制 kid 芯片（点击复制+toast）、大数字余量块（大号「剩余」读数+细进度条 warn/alarm 变色+「已用 X / 上限」说明；金额/Token 各一块，选档优先总额、其次周期未滚动的日/周/月，跨期归零）、caller·并发·口径元信息页脚，悬停微抬升；点击卡片自右缘滑出抽屉（backdrop 模糊、Esc/backdrop/X 关闭、焦点返还打开的卡片），头部标签+状态、整串 kid 复制行、facts 网格、balRow 配额清单与操作按钮原样保留，balanceSeq 竞态守卫沿用并加抽屉打开态校验；状态筛选由 Select 改 chips 按钮组（aria-pressed）；refreshKeys 同步刷新打开中的抽屉（对象被删自动收起）；动画全部带 prefers-reduced-motion 守卫；层级 backdrop/drawer 60/61 避让下拉(70)/toast(80)，原生 dialog 仍在顶层。v0.3.10：密钥界面第四次打磨 + **计费口径域语义变更（金额限额与 Token 限额互斥，二选一）**——service 层在签发与更新（按合并后最终状态）双路径校验，存量双配 Key 在下次编辑时被拦下强制选边；签发/编辑表单新增分段选择器「按金额（USD）/ 按 Token」，只显示并只提交所选族，编辑切换后另一族以 -1 清除；详情从右缘滑出抽屉改回居中原生 dialog（滑出层会盖住宿主 management.html 右上角的四钮控制面板；Esc 经 cancel 接入退出动画、背板点击关闭、::backdrop 模糊、弹入/弹出过渡带 reduced-motion 守卫），dialog 内余额同样对半两块、骨架屏同构预占位原位替换；「签发密钥」按钮移到工具栏最左（不再占用面板头右上角）；卡片标签+kid 芯片同行、余额区对半分（左金额右 Token 竖分割线）、大字「余 X · 已用 Y」一行、进度条贴卡片下缘成刻度带、单档独占整行（.ky-single）；**限额数值语义统一：正数=上限，0=禁用（真实限额，一笔都过不去），-1=不限（与显式 null 同效，签发与更新两条路径均归一）**，编辑表单全量回填当前值废除「留空=不改/null=清除」的隐式规则；balRow 改两行结构（名称+余/上限一行、进度条一行）适配窄半栏；keylimits 测试按单族拆分并新增互斥/-1/0 三组回归钉。v0.3.11：签发/编辑共用的 sheet 弹窗补齐关闭过渡动画（此前只有弹入、关闭瞬时消失）——复用详情 dialog 的 kd-out/kd-fadeout 关键帧，X/取消/确定成功/无 onOk 主按钮四条关闭路径统一走 animateCloseSheet（150ms closing 类后 close），Esc 经 cancel 事件 preventDefault 接入同一动画；openSheet 开场清残留 closing 类并加重开守卫（动画期间重新打开不被挂起定时器误关）。

## 这是什么

面向 CLIProxyAPI 的 **Go `c-shared` 插件**，插件 ID：`cpa-usage-manager`（宿主按动态库文件名派生 ID）。定位：插件 Key（`cum-...`）额度管理为核心 + 单一管理面板展示用量/额度/审计。

- Go module：`github.com/drowsylazy/cpa-usage-manager`
- Go 1.26+，`CGO_ENABLED=1` 才能产出 `.so`/`.dll`/`.dylib`
- 这是**重写**，不是合并：不搬运两插件源码，上游仓库只作实现参考与测试基准
  - 参考源：`AITNR/cap-token-usage-tracker`（统计）、`yuluo688/credit-manager`（Key 额度）

## 已锁定的设计决策（不得违反）

- **存储**：单一 SQLite（`modernc.org/sqlite`，纯 Go），不用 bbolt；单写者 + WAL + 跨进程锁 + handover 租约
- **Key 格式**：`cum-<kid>-<secret>`（不是 `tk-`）；明文仅签发时返回一次，库中只存 HMAC 哈希 + AES-GCM 密文 + pepper
- **金额**：整数 micro-USD，无浮点；只对 Input/Output/Cache Read/Cache Creation 计价，各类别向上取整后相加
- **默认行为**：`quota.enabled=true` 接管前端鉴权；置 `false` 退回纯统计（被动 usage 记录）
- **usage 写入**：单一路径一次入库（逐请求记录 + 分钟聚合 + 账本 + 审计），无双写
- **无公开/自助页面、无会话令牌**：唯一读取面是 `/console` 管理面板，全部数据经宿主管理密钥鉴权的 `/v0/management/plugins/cpa-usage-manager/*`；HTML 壳不含数据；不保留 tracker 的「普通/完整模式」双前端与 `X-Full-Mode-Session`
- **不存上游 API Key 密文**：认证字段只保存清洗后的展示信息，不提供上游 Key 明文回显/标签
- 计价统一为一张 `pricing_rules` 表（match_kind exact/glob/regexp + priority），同时服务额度结算与面板费用展示

## 架构分层

`internal/{config, service, store, money, usageparse, fx, httpapi, web}`，入口为 `main.go`（内联 C ABI，无 cgo 构建标签）。

## 约定

- 推送后**不**监控 GitHub Actions workflow 与发布资产，由维护者自行确认
- 仓库文档与用户沟通使用**中文**。
- 空数据库默认自动建 `default` caller、全模型免费计价规则（`unknown_policy: allow`），但不自动签发任何 Key
- pepper 只在环境变量或 `data_dir/key-peppers`（0600），绝不入库/入日志/入 API 响应
- `data_dir`（0700）备份时须连同 `key-peppers` 一起备份
- 构建产物名必须是 `cpa-usage-manager.{so,dll,dylib}`；本机替换 DLL 后须重启宿主
- 敏感接口（签发/解密/备份/恢复/重置）响应须 `Cache-Control: no-store`
- 面板改动请用拟真数据实测，不要凭空调参：`go run scripts/seed.go` 注入种子（写默认 data_dir，可用 `CPA_DEV_DATA_DIR` 改；固定随机种子、可重复运行），再 `go run scripts/devserver.go` 开 <http://127.0.0.1:18080/console>（管理密钥 `dev-secret`）。`scripts/seed.go` 与 `scripts/devserver.go` 都是 `//go:build ignore` 的开发脚本，不参与构建
- 改图表系列色（`--series-1..4`）必须重跑配色校验（明度带 / 彩度下限 / 相邻 CVD ΔE≥8 / 常视觉 ΔE≥15 / 对比≥3:1），浅深两模式都要过；不要把图表系列色换回品牌语义色（实测两模式均不达标，见项目状态）