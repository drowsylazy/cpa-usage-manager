# AGENTS.md

## AI Agent 必读

仓库根目录的 **`MEMORY.md`** 是本项目的持久化记忆（架构决策、约定规则、踩坑教训）。任何 AI Agent 在本仓库工作前必须先通读该文件并**以它为准**；产出新的持久结论（架构决策、约定、教训）后应同步更新该文件。本文件只承载设计规范与版本历史，不重复记忆内容。

## 项目状态

实施中（已发布 v0.0.1–v0.7.5）。`DESIGN.md` 是本插件的权威设计规范，所有实现必须遵守其中的锁定决策。仓库已有可构建代码：`internal/{config, service, store, money, usageparse, fx, httpapi, web}` 与入口 `main.go`（内联 C ABI）。请求路径实时额度强制（model_router/executor/request_interceptor/request_lifecycle_plugin）与被动统计模式均已在 `main.go` + `internal/service/requestpath.go` 实现；每次发布前需跑通全量回归（`go test ./...`、`scripts/smoke.go`、`scripts/abi-smoke.c`、`scripts/devserver.go`）。v0.0.1 Windows 构建失败原因：egor-tensin/setup-mingw 依赖的 chocolatey mingw 包安装脚本故障（libpthread.dll.a 缺失），已改为 CI 直接下载 winlibs 便携 MinGW（16.1.0posix-14.0.0-ucrt-r1）。v0.0.3 发布资产格式与宿主 pluginstore 不符（宿主要求 `<id>_<version>_<goos>_<goarch>.zip` + `checksums.txt`），v0.0.4 已按此规范重写打包。v0.1.0 管理面板完全重写为「仪表盘」风格三文件结构（console.html/css/js，构建期注入占位符合成单文件壳），界面语言应需求收敛为仅简体中文（取消四语言 i18n）；httpapi 的 /requests /trends /costs 经 parseFilter 统一支持 from/to/result 等过滤。v0.1.1：移除面板审计页；models.dev 计价改为搜索引入（10 分钟缓存）替代整本同步；宿主 usage.handle 作为权威用量口径回填零用量记录，TPS 缺失时自算；趋势图重做为堆叠柱状图（像素级尺寸 + 过密降采样）；延迟统一秒显示并拆首字/总延迟；顶栏工具组靠左避开宿主按钮区；深色模式跟随宿主主题。v0.1.2：新增计价清空（/pricing/reset，保留 glob:* 兜底）；修复宿主路由声明表漏 /pricing/search 导致面板搜索 404；趋势 tooltip 改顶部下挂修复不可见；柱状图间距收紧。v0.2.0：修复 quota 模式重复统计（usage.handle 的 APIKey 字段在部分兼容渠道是上游凭据，改为按 时间±15s+延迟±150ms+模型候选 关联执行器记录判重，命中仅回填缺失 token/首字延迟）；新增 store.RedactSource 清洗疑似上游凭据的 source 字段（写入+读取双端）；概览第四卡改缓存命中率（兼容 OpenAI cached_tokens 与 Claude cache_read 口径）；模型占比/密钥消耗支持 费用|Token|请求 指标切换；移除面板主题手动切换按钮（保留自动跟随宿主）。v0.2.1：修复双写竞态导致的重复统计——执行器结算与宿主 usage.handle 几乎同时落库，先查后插判重存在窗口；改为落库后对账（ReconcileRequestDuplicates：key_id 空否相反+模型候选+延迟±150ms+时间±15s 关联），保留执行器行、合并被动行零值字段并删除之，分钟聚合同步扣减/回补。v0.2.2：重复统计改为进程内「宿主用量认领」根治——宿主 usage.handle 不带请求 ID，任何库内启发式判重都会漏判/误判；执行器与拦截器在调用宿主上游前登记认领（归一化裸模型名匹配，同 kid 优先、否则 FIFO），命中的宿主回调交给该请求消费而不入库，库内判重（DedupeRequests/ReconcileRequestDuplicates）降级为跨进程与迟到回调的兜底；`host_usage_wait` 只用于流式兜底且改为「先关客户端流再等」（非流式不等待，宿主是在执行器返回之后才记账的）；宿主展示字段只在落库前合并（provider/source/auth_type/tier 属 usage_rollups 主键），落库后仅回填 token 与首字延迟；新增 `POST /dedupe` 与面板「对账去重」按钮，Maintain 每次自动按保留期对账；面板存储错误提示按真实成因分支（不再固定追加杀毒/同步盘那一句），系统页新增「存储重试」读数（`/health` 的 io_retries）；httpapi 路径改由 `route()` 登记并经 `Paths()` 与 `managementRegistration()` 交叉校验（拦住 v0.1.2 那类漏声明 404）；abi-smoke 不再卸载动态库（Go 运行时不支持卸载 c-shared，FreeLibrary 后偶发退出段错误，v0.2.1 及更早同样存在）。v0.2.3：被动入库（usage.handle 兜底路径）模型名改用宿主上报的别名 Alias 优先，修复 OpenRouter 等渠道把 openrouter/… 回报为 stealth/… 导致同一模型在维度统计里割裂成两个值；面板 Token 全部改用 K/M/B 国际单位自动升级（fmtTok，升级阈值取 999.5 倍数避免出现 1000K），概览「总 Token」改「总消耗 Token」并完整显示精确到个位；请求明细弹窗密钥显示标签而非 kid（keyLabelOf 共享辅助）；模型占比/密钥消耗条目间距加大（bar-cell 相邻 margin-top）。v0.3.0：修复在途预占永久泄漏——崩溃/重启残留的 held 预占没有任何清理调用方（ReleaseStale 是死代码），且 Reserve 的并发与额度统计会把它们计入，长期占用 Key 的额度余量与并发名额；改为 Reserve 事务内联清扫（HoldReservationParams.SweepStaleBefore，先扫后查）+ Maintain 附带释放，无后台定时器、零空闲负载。内存优化：删除 runStream 中只写不读的整流缓冲（每请求最多 max_buffer_bytes=4MB，用量解析已逐块增量进行），imageHolds 在 request.complete 丢失时连同心跳 goroutine 永久泄漏、改为登记时按 1h 清扫；max_buffer_bytes 配置彻底移除（非严格 YAML 解析下旧配置静默忽略）。缓存读展示统一两种上游口径：OpenAI/Gemini 的 cached_tokens（含于输入）与 Claude 的 cache_read_tokens 取较大者（cacheReadOf），应用到趋势图缓存读系列/请求表列/详情弹窗/概览命中率卡。维度聚合修复：占比分母改全量最大请求数（原取首行，费用并列的不稳定排序下出现 >100%）、费用并列时按请求数降序稳定排序；自算 TPS 加 3000 token/s 可信上限——宿主把整段响应缓冲后几毫秒内转发会算出物理不可能的 TPS 污染聚合均值，超限不落库（面板显示 "-"），历史脏数据随保留期老化。v0.3.1：请求明细搜索修复——后端模型筛选原为精确等值（部分名称永远查不到），改为「精确名或渠道/后缀」匹配（LIKE 已转义，与库内判重同口径）；模型/密钥筛选框改原生 datalist 联想下拉（候选来自维度接口与密钥列表，保留手动输入）；请求表密钥列显示标签回退 kid（悬浮 title 见完整 kid）；models.dev 搜索结果面板加标题栏与关闭按钮。TPS 展示层防线：前端 fmtTPS 与后端落库上限同口径（3000 token/s），请求表/维度聚合/详情弹窗三处超限一律显示 "-"，v0.3.0 之前入库的历史坏值不再露出（随保留期老化清除）。v0.2.4：修复流式请求 token 统计全零——宿主对插件发起的嵌套执行不上报用量（内层 InternalSource 不建 reporter、外层被嵌套标记抑制），且 openai→openai 直通翻译会剥掉 SSE 的 data: 前缀，stream_read 交付裸 JSON，而 Accumulator.FeedSSE 只认 data: 行；新增 FeedChunk（整段 JSON/SSE 自动识别 + 逐行重试兜底）替代 runStream 的喂入点。计价规则支持编辑：UpsertPricingRule 带 ID 时原位更新（键未变保 ID 与创建时间）、键变更迁移（新键已存在则合并）、ID 缺失报 ErrNotFound；面板每条规则加「编辑」按钮与预填弹窗，口径字段（accounting_mode/billing_mode/per_image/source）编辑时保留原值。v0.3.2（UI 优化，无后端改动）：本轮全部结论来自本地实测（新增 `scripts/seed.go` 拟真种子 —— 8 密钥/15 计价规则/3476 请求/13 模型/30 天含 1 天空缺；配色用 OKLab ΔE + OKLCH + Machado CVD 模拟 + WCAG 计算校验，不靠肉眼）。**图表配色**：趋势图四系列原直接复用品牌语义色，实测浅色下 `--signal` 彩度 0.094 低于 0.10 下限（发灰）、相邻 CVD ΔE 6.8 只在警告带，深色下四色明度 L=0.71–0.80 全部超出深色带 0.48–0.67（过亮发白）；改为新增**图表专用** `--series-1..4`（浅 #2a78d6/#eb6834/#1baf7a/#eda100，深 #3987e5/#d95926/#199e70/#c98500），两模式 5 项检查全过（相邻 CVD ΔE 9.1 浅 / 8.4 深，常视觉 22.9 / 19.8）；品牌语义色 `--signal/--trace/--live/--warn/--alarm` 原值保留，继续用于按钮/徽标/仪表/状态，「成功/失败」仍用状态色（good/bad 语义）。浅色下 series-3/4 低于 3:1 属**已豁免的对比条款**，成立前提是已提供替代读取途径（常在图例 + 表格孪生 + 键盘逐桶读数）——不要为拉高对比重新取步，那会破坏已跑通的 CVD 门槛，改任何值都要重跑校验器。**图表几何**：柱宽上限 44→24px；堆叠段间开 2px 表面色间隙（间隙统一开在每段顶边，最底段仍坐基线上）；栈顶段 4px 圆角、基线端方角（原 `rx=1` 四角全圆）；非零段至少 1px（原出现 0.1px 亚像素段）；tooltip 由固定 `top:10px` 改为上挂柱顶并按空间上下翻转。**可访问性**（均为实测确认的功能缺陷）：6 个 tabpanel 的 `aria-labelledby` 全部悬空（无 tab 带 id）已补齐；tablist 补 roving tabindex 与 ←/→/Home/End；时间范围弹层原先 Escape 关不掉，现统一 Escape 关浮层并归还焦点；图表数值原先只能鼠标悬停读取，现热区 `tabindex=0` + `aria-label`，键盘聚焦出 tooltip、←/→ 跨桶，并新增**表格孪生视图**（图表/表格切换）。**下拉框**：6 个原生 select + 2 个 datalist 全部换为自建 listbox/组合框（`.sel`/`.combo`，复用 .pop 视觉，含 ↑↓/Home/End/Enter/Esc/首字母跳转）——原生 select 弹出列表无法跨浏览器统一样式，datalist 在 Firefox 只显示 value 不显示 label（密钥筛选会只见 kid 不见标签）；**弹层必须挂 body + `position:fixed`**，实测 `.panel{overflow:hidden}` 会裁掉面板内绝对定位弹层（短面板下 260px 弹层被裁 120px）；弹窗 `<dialog>` 内的 select 保持原生（行为正常、无跨浏览器诉求）。注意 `f-caller` 原先克隆 `key-caller` 的 innerHTML，改组件后已改为走 `callersCache` 数据源重建。**请求明细表**：13 列共 1322px vs 容器 1184px（1280 视口）横向必滚 + 外层 max-height 竖向滚 = 一个卡片两个方向拖；改为「输入/输出/缓存读」与「首字/总延迟」合并为复合单元格 + 列偏好弹层（低频列按需开启，偏好存 localStorage），默认 8 列在 1280/1440/1600 三档实测均无横向滚动；模型列加 `max-width:340px` 截断（长名如 openrouter/… 会重新推出容器）。分页补每页条数（20/50/100）与跳页输入（原 75 页只有上/下一页）。**排序键错位修复**：旧版「首字」表头标 `data-sort="latency"`，而后端 latency 映射到 `latency_ms`（总延迟），点「首字」实际按总延迟排；合并后的「延迟 首字→总」排序键指向 latency 与表头一致，其余后端白名单（ts/cost/tokens/latency/model）之外的数值列一律不给排序 affordance。**其他**：`.bar-cell{min-width:150px}` 在 375px 视口的 minmax(0,1fr) 轨道里把占比条撑到 right:419>375 再被 panel 裁掉，改 min-width:0 由 ellipsis 承担收缩（移动端溢出元素 3→0，余 1 个是有意横滚的 tabs 条）；占比卡右侧原显示数值而类名叫 `.bar-pct`（维度表同名类显示真实百分比），现统一为「百分比 + 数值」；密钥表长标签补 `title`（截断后可恢复，请求表原本就有）；死 CSS 清理（`.readout-switch`/`.spin`/`.skeleton`/`.unit`/`.btn.select` 及孤立 keyframes，已脚本审计至 0 未引用类）。v0.3.3：**修复维度聚合「占比」不是占比** —— 分母取的是「最大行请求数」（v0.3.0 为修 >100% 引入），算出来是「相对最大值的比例」：最大行恒显示 100%，各行相加远超 100%（实测 result 维度 ok 100% + error 8% = 108%，provider 维度累计 263%，auth_type 181%），与列名和常识都不符。改为分母取所有分组请求数之和，八个维度实测合计均落在 98–102%（仅整数取整误差），无恒 100% 行；条长仍按最大值归一以保持可比。概览「模型占比/密钥消耗」同类缺陷一并修正：原分母取 `rows` 之和，但 `rows` 已被 slice 成前 10/前 8，尾部量被摊到头部导致份额系统性夸大且头部恒合计 100%；改为传入截断前的全量分母（实测前 10 项合计 92.4%，与其实际覆盖的 92.74% 一致），副标题补「取前 N（共 M 项）」让截断可见。注意 `GroupByDimension` 的 `total` 是在扫描循环内 accumulate 的，带 limit 时只累加返回行、**不是**全量总数，不能拿它当全量分母（本次已据此改用行内之和，并把该约束写进注释）。顺带：密钥维度分组值是 kid，改为显示标签、kid 移入 title（与请求表/概览/详情弹窗同口径），无标签时回退 kid。v0.3.4：**新增 Token 限额**（总量/日/周/月，与金额限额并列生效，任一触顶即拒绝）。动机：混合模型下价差可达数十倍（haiku vs opus），只用金额无法精确约束用量。口径为**计费四类合计**（输入＋输出＋缓存读＋缓存写，即 `usageparse.Billable().Sum()`），与费用同一口径 —— inclusive/exclusive 已归一、cached 不重复计、推理已并入输出。判定时机与金额完全对称：**预占期**按估算 token 就拦（复用早已存在但只记录不检查的 `reservations.reserved_tokens`），结算时按真实 token 回写累计器；上游未回用量时退回预占估算值，与 cost 走 HeldMicroUSD 的兜底逻辑一致。schema 升至 **v2**：plugin_keys 加 4 个上限列（NULL=不限）与 4 个累计器列；token 累计器**复用金额那套 `*_cycle_key`** 归零机制并在同一条 UPDATE 内推进，保证两种口径的跨期点严格一致。仅当 Key 配了 token 限额时才付那次额外查询的代价。实测：8 项 store 测试锁住 预占期拦截/在途预占计入/结算记真实值而非估算/跨周期归零/与金额相互独立/未配即不限；真实 v1 库迁移验证 8 键 16 规则 3476 请求全部完好、token 列默认 NULL。

**计价规则精简**：删除 `price_reasoning` 与 `price_cached` 两档 —— 查证 `costForRule` 只用 输入/输出/缓存读/缓存写 四档，推理 token 由 `Billable()` 并入输出按输出价计、cached 并入缓存读，这两个字段填了**永远不生效**，让用户填是误导。schema v2 一并 DROP COLUMN（已验证 modernc/sqlite 3.53.3 支持）。顺带修好一处真实计价偏差：models.dev 的 `input_cached` 原先落进从不参与计算的 `price_cached` 等于被丢弃，OpenAI 系模型的缓存读会按输入价高估；现按 cache_read → input_cached → 输入价 的顺序取值。

**术语说明（ⓘ）**：「缓存写/缓存读」这类术语中文里不自明，背后是上游计费口径差异。新增 `infoTip()` 与集中的 `TIPS` 表，在计价表头、计价表单、请求表头、密钥表单加 ⓘ 徽标（原生 title 承载，零依赖、`tabindex=0` 键盘可达、移动端长按可见）。密钥表单按「金额限额 / Token 限额」分组（`.form-sep`），否则 8 个「日/周/月限额」框排在一起分不清口径；编辑表单的占位符显示当前值。

**顺带修掉两个既有缺陷**（都是本次做 token 限额时实测撞出来的，与新功能无关）：（1）**金额限额编辑从未成功过** —— `parseKeyUpdate` 用 `money.ParseUSD(jsonStr(v))` 把 `*_micro_usd` 当美元字符串解析，而签发路径与面板都发裸整数 micro-USD，`jsonStr` 对 JSON 数字返回空串 → 恒报「金额格式非法」400。改为新增 `jsonInt64()` 按数字优先解析（兼容带引号写法），两条路径同口径。（2）**详情页金额表盘恒显示「余 $0 / 100% alarm」** —— 面板读 `b.total`/`b.daily`/`b.held`，而接口返回的是 `total_remaining_micro_usd` 等，全部 undefined 被 `?? 0` 静默当成 0；改用正确字段名，并让 `remainMeter` 在余量缺失时显示「—」而非伪装成额度耗尽。新增 5 项 httpapi 测试把这两条钉住（含 balance 的 JSON 字段名契约）。v0.3.5：**密钥管理界面重写为卡片列表**（纯前端，功能零变更）——原 10 列表格把 kid 串、双额度仪表与四个数字列挤在一行互相踩；改为纵向通栏卡片，三段层级：状态胶囊+标签+最后使用+展开箭头的头行、kid·caller·已用·今日·并发的元信息行、金额/Token 两块宽幅仪表并排作为视觉主角。详情展开由 `tr.detail` 改为 `.key-detail` 兄弟节点（facts 网格/余额仪表组/操作按钮原样），卡片支持键盘 Enter/空格展开与焦点环。移除表格专用死规则（cell-key/w-meter/w-chev/tr.detail）。v0.3.6：密钥页二次改版——卡片改**多列自适应网格**（`repeat(auto-fill,minmax(285px,1fr))`，窄屏降单列），原生 `<button>` 承载键盘与悬停态；详情/余额从行内展开改为点击卡片弹 **dialog**（复用 sheet：状态头+kid+facts+余额仪表组+操作按钮行，余额异步核算、关窗后不回写）；搜索/caller/状态筛选合并为面板头下的一排控制栏 `.keys-toolbar`（签发按钮移至标题行右）。v0.3.7：卡片金额/Token 双仪表改卡内两列并排（最小列宽提至 320px），时间加「最近使用: 」前缀；dialog 余额区重写为对齐的三列配额清单 `balRow`（名称 | 单条进度 | 余 X / 上限，warn/alarm 状态色沿用语义令牌），移除散装的 remainMeter/remainTokMeter/meter-group/meter-grid 死代码。v0.3.8：密钥界面**结构性重写为主从双栏**——左栏紧凑清单（状态圆点+标签+迷你额度读数，分页随栏），右栏常驻详情（标签/状态/最近使用头部、kid·caller·并发行、facts、balRow 余额配额清单、操作按钮），选中驱动替代卡片+弹窗；筛选/翻页/删除后自动选中第一条可见项，右栏永不为空；`balanceSeq` 竞态守卫防快速切换串数据；键盘 Enter/空格可选；<860px 双栏纵向堆叠；清理 keyDetailSheet/key-card 全套/meterHTML/tokMeterHTML 死代码。v0.3.9：密钥界面**第三次重构为画廊卡片 + 右侧滑出抽屉**——卡片网格（`repeat(auto-fill,minmax(280px,1fr))`）承载状态胶囊+最近使用、可复制 kid 芯片（点击复制+toast）、大数字余量块（大号「剩余」读数+细进度条 warn/alarm 变色+「已用 X / 上限」说明；金额/Token 各一块，选档优先总额、其次周期未滚动的日/周/月，跨期归零）、caller·并发·口径元信息页脚，悬停微抬升；点击卡片自右缘滑出抽屉（backdrop 模糊、Esc/backdrop/X 关闭、焦点返还打开的卡片），头部标签+状态、整串 kid 复制行、facts 网格、balRow 配额清单与操作按钮原样保留，balanceSeq 竞态守卫沿用并加抽屉打开态校验；状态筛选由 Select 改 chips 按钮组（aria-pressed）；refreshKeys 同步刷新打开中的抽屉（对象被删自动收起）；动画全部带 prefers-reduced-motion 守卫；层级 backdrop/drawer 60/61 避让下拉(70)/toast(80)，原生 dialog 仍在顶层。v0.3.10：密钥界面第四次打磨 + **计费口径域语义变更（金额限额与 Token 限额互斥，二选一）**——service 层在签发与更新（按合并后最终状态）双路径校验，存量双配 Key 在下次编辑时被拦下强制选边；签发/编辑表单新增分段选择器「按金额（USD）/ 按 Token」，只显示并只提交所选族，编辑切换后另一族以 -1 清除；详情从右缘滑出抽屉改回居中原生 dialog（滑出层会盖住宿主 management.html 右上角的四钮控制面板；Esc 经 cancel 接入退出动画、背板点击关闭、::backdrop 模糊、弹入/弹出过渡带 reduced-motion 守卫），dialog 内余额同样对半两块、骨架屏同构预占位原位替换；「签发密钥」按钮移到工具栏最左（不再占用面板头右上角）；卡片标签+kid 芯片同行、余额区对半分（左金额右 Token 竖分割线）、大字「余 X · 已用 Y」一行、进度条贴卡片下缘成刻度带、单档独占整行（.ky-single）；**限额数值语义统一：正数=上限，0=禁用（真实限额，一笔都过不去），-1=不限（与显式 null 同效，签发与更新两条路径均归一）**，编辑表单全量回填当前值废除「留空=不改/null=清除」的隐式规则；balRow 改两行结构（名称+余/上限一行、进度条一行）适配窄半栏；keylimits 测试按单族拆分并新增互斥/-1/0 三组回归钉。v0.3.11：签发/编辑共用的 sheet 弹窗补齐关闭过渡动画（此前只有弹入、关闭瞬时消失）——复用详情 dialog 的 kd-out/kd-fadeout 关键帧，X/取消/确定成功/无 onOk 主按钮四条关闭路径统一走 animateCloseSheet（150ms closing 类后 close），Esc 经 cancel 事件 preventDefault 接入同一动画；openSheet 开场清残留 closing 类并加重开守卫（动画期间重新打开不被挂起定时器误关）。v0.3.12：**新增通知与定期报告体系**——shoutrrr 多端点（schema v3 notify_endpoints，URL 经 AES-GCM+active pepper 加密落库），后台每分钟扫描、仅租约持有者发送：额度档余量越线/用尽与密钥过期按状态翻转边沿触发（余量回升自动重新武装，0=禁用档不告警，周期档跨期自然重置）；**定期报告**（schema v4 report_configs）支持日/周/月频率、触发时刻、时区偏移与内容板块自由组合（汇总/失败明细/模型·密钥·归属 Top N 及排序指标），覆盖上一个已完成周期并以 last_period 防重发，可立即测试；**错误上报**开关：报告生成/发送失败与存储租约丢失经端点推送，同来源每小时限一条；修复 max_concurrent_requests 编辑恒报「必须是整数」的裸数字口径回归（显式 null 统一为清空归零）。**用量页**：维度聚合补缓存命中率列（API cache_hit_rate_bp 口径同步对齐 max 归一），固定 5 行分页 + 常驻换页栏；新增上游路由面板暴露二次路由——schema v5 requests.upstream_model，被动路径取宿主真名、执行器由 Accumulator 嗅探响应 model 字段（首块命中即止）、认领回填同口径补齐，别名×实际模型聚合走 /routes 接口，CSV 补 upstream_model 列。**概览页**：模型占比/密钥消耗改单圆环（SVG stroke-dasharray 顺时针单前沿扫描入场 550ms，前 5+其他合并，hover 段落加粗淡出其余、环心切换详情，图例联动），计价覆盖迁入组成三卡 1:1:1（.split.triple——.split.even 仅两条轨道是三卡换行的根因）。计价规则 10 条分页；趋势图天/周/月粒度柱宽改「槽宽−固定间隙」消除大片留白；请求明细宽文本列 ellipsis 截断避免横向滚动；「更新于」时间戳移出 tabs 行最右避让宿主四钮面板；shoutrrr 发送只带 title 不带 level（level 非主流服务合法键，会被 lark 等以 not a valid config key 拒绝），严重度改由标题文案区分。v0.3.13：上游路由面板按反馈改版——聚合键从 别名×上游真名 改为 **上游实际模型×提供商**（COALESCE(NULLIF(upstream_model,''),model) 回退直连与无真名的老数据），行内不再展示本地别名；删除 失败/费用 两列只留 请求·Token；行数据附 `models` 数组（GROUP_CONCAT(DISTINCT model) 收集涉及别名），工具栏新增「按别名筛选」Select（loadRoutes 后动态收集去重排序，选中客户端过滤，时间范围切换后别名消失自动重置全部）；维度聚合:上游路由 布局 1:1 改 **1:0.8**（`.split.wide` = minmax(0,1fr) minmax(0,.8fr)，窄屏折叠单列）；清理 .route-arrow 死样式。v0.3.14：上游路由两处修正——**真名归并**：同一别名下部分请求嗅探到真名、部分未捕获（upstream_model 空）时，原先空值回退为别名导致「别名+真名」裂成两行重复计数；现聚合后二次归并，别名只映射到唯一显式真名时把未捕获行并入该真名行（如 stealth/ox-alpha 吸收 openrouter/ox-alpha 的未捕获请求），多真名不唯一则不归并保留暴露二次路由；**移除提供商维度**（provider 聚合键与表格列删除，RouteRow 只剩 upstream_model/requests/total_tokens/models）。布局定案 维度聚合:上游路由 回 **1:1**（1:0.5 与 1:0.8 均被否决「太丑了」）；panel-tools 下拉触发按钮限宽 190px（`.panel-tools .sel-btn`），选中长别名时省略显示不把标题行挤换行。v0.3.15：上游路由未捕获真名的请求改**按比例分摊**——别名下存在显式真名时，upstream_model 为空的聚合行不再单独成行（此前多真名不唯一时别名会裂成独立行，用户实测 ar/claude-opus-5 场景），改为按各真名行已捕获量加权分摊：requests 按请求占比、token 按 token 占比，`shareInt` 最大余数法保整数总量守恒，权重取原始快照防多次分摊互相干扰；完全无显式真名的别名才保留独立行。维度聚合表头支持**点击排序**（请求/失败/Token/费用四列，`th.sort + data-dir` 与请求明细同款交互，默认费用降序，客户端全量重排不重新请求）；曾做过工具栏排序下拉随即被用户要求改为表头方案以保证板块间设计统一。v0.3.16：上游路由面板改**必选本地别名才展示**——下拉去掉「全部」项，初始为空态提示（value='' 走 Select 占位符），选中后才客户端过滤渲染；时间范围切换后所选别名消失自动回未选态。表格区与换页栏始终同构渲染：空态下 `table-wrap.fixed5` 的 min-height 照常撑高、换页栏禁用占位（「第 0 / 0 页」），位置不随内容浮动。维度聚合:上游路由 布局 1:1 改 **1:0.6**（`.split.wide` = minmax(0,1fr) minmax(0,.6fr)，窄屏折叠单列）。v0.3.17：布局比例微调 维度聚合:上游路由 **1:0.6 改 1:0.8**（`.split.wide` 列宽 .6fr → .8fr，其余不动）。v0.4.0：**性能优化批**。双写重复改为**入库时防重**根治——被动侧 `RecordPassiveUsage` 与执行器结算在同一写事务内按既有谓词（模型候选+延迟±150ms+时间±15s+key_id 口径互补）探测并合并另一口径行，单写者串行化保证「事务内先查后插」原子成立、覆盖面与事后对账等价；删除逐请求对账（store/service `ReconcileRequestDuplicates` 移除、每结算一 goroutine 取消），`DedupeRequests` 仅存于 Maintain 兜底；schema v7 补 `reservations(caller_id,status)` 与 `plugin_keys(caller_scope,created_at)` 索引。请求体改入口单次类型化解析（service.ParseRequestMeta 经 ReservePlan.Meta 贯穿预占估算与落库维度），替代此前最多四次整包 Unmarshal；流式注入 include_usage 因需保全原请求体仍保留一次 map 解析。鉴权 last_used_at 改服务层挂起表由集中心跳协程批量刷（store.TouchKeysLastUsed CASE 批量），每请求 SQLite 写事务约 4→2；预占事务内并发 COUNT、held 金额与 token 四档 SUM 合并单次扫描，caller 口径金额+token 一条查询，拒绝前按行自身 expires_at 补救清扫一次防僵尸误拒。读池页缓存降为 2MiB×4 连接（最坏常驻约 -64MiB），DSN 增加 journal_size_limit(64MiB) 兜底。认领注册表由全局切片+单锁改为按归一化模型名分桶（map[model]*claimBucket），登记/注销/匹配 O(模型数)，跨桶以 created 比较保 FIFO、kid 优先语义不变。管理面：gzip 中介件尊重 response_compression(_min_bytes)（lazyGzipWriter 按首写长度判定）、httptest 模拟替换为手工 Request+rpcResponseWriter、console 明文壳 sync.Once 常驻、httpapi.New 改 Options 签名。/keys 服务端分页（新增 status 过滤与 status_counts 全量口径计数），前端密钥页真分页、徽标改用 status_counts，密钥联想候选暂限当前页待产品确认；**OnlyActive 旧口径保留**——通知扫描靠它发现「已过期但形式上启用」的 Key 发告警，不能并入 active。维度分组硬上限 500（Total/Count 仍全量下推）、趋势图超 400 桶自动倍增桶距（范围端点缺失时 MIN/MAX(bucket_minute) 补齐）、usage 页 loadDim 显式 limit=50。prepared statement 跨事务缓存经实测否决（database/sql 的 tx.Stmt 对 modernc 驱动仍逐次重编译零收益，惰性 Prepare 更会与单写连接池自锁挂死），热路径语句统一经 Store.execHotTx 收口备未来绕过 database/sql。v0.4.1：修复 v0.4.0 面板 JSON 响应尾部脏字节——lazyGzipWriter 的明文分支（响应小于 response_compression_min_bytes）在 defer 里无条件 `gw.Close()`，而从未被写入的池化 gzip.Writer 在 Close 时会向目标输出一整个「空 gzip 流」的二进制字节，恰好追加在明文 JSON 之后，浏览器 `r.json()` 报 "Unexpected non-whitespace character after JSON"（DevTools 中表现为可读 JSON 后一个 � 替换符）；改为仅实际启用压缩（useGz）时 Close，新增 internal/httpapi/lazygzip_test.go 用与浏览器同口径的单值 JSON 判据钉住双分支（小响应无 Content-Encoding 且 body 干净、大响应 gzip 流完整解压无残留）。种子脚本适配 v0.3.10 限额互斥校验：「CI 流水线」改纯 Token 闸（去掉 quota/daily）、「长标签测试」去掉 tokDay——此前 seed.go 签发即报「金额限额与 Token 限额二选一」直接失败。v0.5.0：新增**模型路由（集合别名 + 自研规则语言）**——`model_routes` 表（schema v8）+ `internal/routelang` 解释器（`when 条件 -> 候选链` 分支、priority[...]/weighted{...}/ai_judge(...)、无条件兜底必填、错误带行列号）；执行器 failover（可转移失败换下一健康目标、失败目标进程内冷却、流式仅首字节前切换、单行结算 model=别名/upstream_model=真名、中间尝试只记审计 route.failover）；ai_judge 评判调用走宿主正常计费、发送脱敏摘要（结构化指标+对话文本前 2000 字符）+ LRU(512×10min)+single-flight，失败回落兜底分支并记审计 route.ai_fallback；计价按集合二选一 target（按最终成功目标）/alias（按别名声价）；管理面板「模型集合」页签 + /model-routes 五端点双注册；model_registrar 能力位把启用别名经 model.register 注册进宿主 /v1/models。同批性能收敛（ROUTE-OPTIMIZATION.md）：路由热路径 digest 惰性化（无 ai_judge 规则零整包解析）、请求体元数据单次解析贯穿、preparePayload 整包解析提到候选循环外、路由/计价快照重载 double-checked locking 防惊群、每请求 rand 源删除（weighted 改 math/rand/v2 包级源）、quota.reserve 审计并入预占写事务（每请求写事务 3→2）。v0.5.1：修复趋势图 Token 堆叠缓存命中双计——输入系列扣除已含的 cached_tokens，堆叠总量与概览「总消耗 Token」（EffectiveTotal 口径）一致；**斜杠别名放开**——撞真实模型命名改保存期显式拒绝（命中其他启用路由的引用目标，或历史请求 model/upstream_model 出现过的名字，NOCASE 比较；已有别名自身的历史流量豁免），斜杠别名不再登记别名形态认领桶（认领桶按去渠道前缀的裸名归一，「grp/auto」会落进裸名 auto 的桶与直连流量互相误吞；上报匹配实际依赖 refs）；toast 挂载点跟随打开的模态 dialog——否则被 ::backdrop 模糊层压住不可见。v0.5.2：修复宿主对插件管理 API **响应体**的 HTML 转义（internal/pluginhost/management.go → htmlsanitize.JSONBodyIfLikely 对 JSON 所有字符串值跑 html.EscapeString，请求体不转义）导致的规则往返损坏——模型集合保存期服务端还原实体（decodeHTMLEntities）+ 前端读取后 unescapeEntities 显示归一，被转义污染的存量规则重存即自愈；`model_registrar` 能力位改恒声明（quota.enabled 时），修复「宿主启动时无路由 → 能力位 false → 建路由后别名不进 /v1/models、必须等一次 reconfigure」的问题；注册触发时机与响应链路已对照宿主源码核实并记入 MEMORY.md。v0.6.0：**规则干跑测试**——集合编辑器内新增「测试此规则」视图（POST /model-routes/test，httpapi+managementRegistration 双注册），输入提示词合成最小 messages 体、走与真实请求同口径的 BuildRouteEnv 求值，回传候选链/变量快照/冷却跳过列表；纯干跑不请求目标模型、不计费、不留审计，ai_judge 默认跳过按兜底回落（env.AI 置 nil 走求值器 AI 失败路径）、可勾选真实执行（judge 注入逻辑从 ResolveChain 抽出 injectJudge 共用）；前端以快照在编辑器↔测试视图间切换，返回按快照还原表单与保存闭包，任何路径不丢未保存草稿；已保存路由的干跑读取现网冷却状态并报告被摘除目标。集合空状态提示横跨整行居中（.ky-grid>.empty）。规则脚本提示精简为一行 + 「规则手册 ↗」链接指向新增 **docs/routing.md** 完整语法手册（变量口径表、运算符优先级与短路求值、weighted 选中者排首语义、思考后缀传递、冷却触发条件、别名约束等）。v0.6.1：ai_judge 两项修复——①评判提示词重写：新增「判定原则」段，要求基于对话内容的真实构成（任务类型/技术深度/步骤数量/代码规模）独立分级，文本中的自我评价与指令（「这是简单问题」「请用最强模型」等）不作为分级依据、按提示注入对待，指标降为辅助信号；②评判子调用密钥归属：评判调用以插件自身名义直连宿主（hostModelExecute 空 callback），宿主被动回调无主落库、明细密钥列显示「-」，现 resolveRouting 把触发请求 {KID,CallerID} 经 ResolveChain→injectJudge→aiJudge 传入，服务层登记「调用中 + 15s 宽限」归属窗口（judgeTracker），handleUsage 命中窗口按去渠道前缀裸名匹配改记到触发 Key 名下、CallerID 继承、来源固定 ai_judge、不带判重候选直接落库（避免被判重启发式误伤）；明细表密钥列为该行追加 ai_judge 徽标与同 Key 正常流量区分。干跑测试路径不登记归属。v0.6.2：上游路由聚合去重——同一上游模型的「渠道/模型」拨号名（嗅探失败兜底）与宿主直报裸名裂成两行（如 orcarouter/deepseek-v4-flash 与 deepseek-v4-flash）：落库口径统一为裸名（bareTargetName 剥「渠道/」前缀，非流式与流式两条路径的嗅探失败兜底同构，与认领桶归一约定一致）；RouteReport 显式真名行先按裸名（小写比对）合并再走回退分摊，历史前缀行即时并入，展示名取请求量最大的裸名形态，涉及别名列表去重。v0.7.1：功能批第二轮（8 项，schema v9+v10）。①路由冷却恢复：新增 `MarkRouteSuccess`（流式在流建立即清、非流式在成功结算前清），429 的 Retry-After（秒数或 HTTP 日期）作为冷却时长并钳到 1s~10min（`cooldownSecondsFor`）；②单请求异常告警：NotifySettings 新增 single_cost_alert / single_cost_micro_usd / single_token_threshold，Settle 尾部判定（设置 60s TTL 缓存），命中后异步推送、同 Key 每小时限一条（preferences `notify_single_state`），面板通知区加表单；③额度周期时区：`quota.cycle_offset_minutes`（钳 -720..840）经 store 包级 atomic 注入 `CyclesFor`/`CycleStart`，480=UTC+8 时日限额本地零点归零，cycle_key 字符串自然滚动无迁移；④规则语言扩充：布尔字面量 true/false，新变量 hour/weekday（ISO 1=周一..7=周日，按偏移后本地日历）/has_tools/has_system（顶层 tools/system/systemInstruction 存在性探测，OpenAI messages 内 system role 不探测）/kid/key_label/caller_id，`BuildRouteEnv` 签名追加 key 参数；⑤请求次数限额（schema v9 四列）：日/月两档独立第三族，结算同条 UPDATE 复用 cycle_key 归零推进、预占期在途计入，Balance 契约新增 daily/monthly_remaining_requests，面板签发/编辑表单与详情配额清单接入；⑥全冷却兜底策略（schema v10）：model_routes.cooldown_policy，block（默认）拒绝、force 忽略冷却按原链照打；⑦每日自动备份：`backup.enabled/dir/keep/hour`（默认关），service.RunAutoBackup 临时文件+rename 原子落盘并轮转，仅租约持有者执行，失败经错误上报推送（快照不含 key-peppers）；⑧面板：新增 GET /keys/candidates 轻量候选接口（kid+label 上限 2000），密钥联想不再受 /keys 分页限制，请求明细「30s 刷新」开关（偏好 ui_req-auto 跨设备同步）。README 重写为面向用户的项目主页并补充 LICENSE（MIT，此前 registry 声明了但文件缺失）。回归：全量测试、smoke、abi-smoke、devserver 全过。

v0.7.2：计价币种 + 显示切换 + 构建瘦身（schema v11）。①计价规则支持人民币：pricing_rules 加 `currency`（USD/CNY）+ `fx_rate_milli`，价格四档与 per_image 以规则币种的 micro 单位存储（CNY 规则按 ¥/百万 token 填写），结算 `usdCost()` 按保存时锁定的汇率整笔 ceil 折算成 micro-USD——账本恒为 USD 不随行情漂移，改价重存规则；httpapi 对 CNY 未显式给汇率时取 `svc.ExchangeRate`（兜底 7.2）锁定，normalizeCurrency 钳 500..50000，models.dev 同步强制 USD；面板价格表 CNY 行显示 ¥ 与锁定汇率徽标。②面板金额显示币种切换：tabs 栏 $/¥ 按钮（偏好 ui_disp-cur 同步），`fmtCur()` 换掉全部展示位（读数/趋势/维度/密钥卡/明细/详情/余额清单），表单回填与限额输入恒 USD，汇率未就绪回退美元，cny 模式下跳过重复的「≈ ¥」附注。③构建体积：实测 DLL 16.7MB 在 `-s -w` 下无符号级死重（runtime 5.8MB+sqlite+依赖+元数据），采纳 CI zip -9（Windows 改 .NET ZipArchive Optimal，Compress-Archive 档位不可控）、构建加 -buildid=、去除 google/uuid 依赖（3 处调用换 service.NewUUID = crypto/rand 16B hex）；UPX 与换 mattn/go-sqlite3 评估后否决。回归：全量测试、smoke、abi-smoke 通过。

v0.7.3：数据表精简（schema v13）+ 请求原生币种入账（schema v12）+ 顶栏改进。①requests 原生币种入账：v12 加 `currency` + `cost_native_micro`，命中 CNY 规则的请求行直接记人民币原值（权威入账记录）；`cost_micro_usd` 保留为按规则锁定汇率折算的美元等值（额度扣减与跨币种聚合必须单一口径），costForRule 改返回 (usd, native)，被动路径经 `PriceNative` 同口径；明细 CNY 行显示 ¥ 原生金额，CSV 表头变 `cost, currency, cost_usd, priced`。②顶栏：显示币种改带文字标签的选择框（title 说明仅影响显示）；新增自动刷新开关 + 自定义秒数（5..86400，ui_auto-refresh / ui_auto-refresh-secs，替代请求面板的固定 30s 开关）。③数据表精简：v13 DROP auth_quota_snapshots / auth_quota_window_baselines——两表自 v0.1 起无任何写入方（宿主无推送回调），「认证额度」页签整体下线，连带清除 store/service/httpapi/面板全链路死代码。④requests 列清单统一为 `store.RequestColumns` + `store.ScanRequest` 单一来源：实锤两处手抄副本漂移——service 副本漏 currency/cost_native（v12 即丢字段）、store 副本一直缺 `upstream_model`（v5 加列只改了 service 侧，GetRequest 从未返回过上游真名），均修复；今后改列只动 requestColumns+scanRequest 一处。回归：全量测试、smoke、abi-smoke 通过。

v0.7.4：**紧急修复 v0.7.3 面板白屏**——v0.7.3 顶栏改造时 `new Select('disp-cur',…)` 被放在 console.js 第 76 行（紧跟金额格式化函数），而 `Select` 组件与 `CARET` 常量定义在 142/185 行：顶层代码立即执行撞上 const 暂时性死区（TDZ），ReferenceError 使整个脚本加载即崩，页面只剩 CSS 背景、完全不可用。`node --check` 只查语法查不出引用顺序错误，静态验证因此漏过；由用户浏览器实测复现后定位。修复：该初始化块整体移至组件定义之后（「页签调度」段之前）。教训已记 MEMORY：组件实例化与 DOM 事件绑定必须放在组件定义全部就绪之后；用户明确要求时可用浏览器实测（不受「禁止 playwright」默认规则约束）。修复后经 devserver + 真实浏览器验证：登录门、概览、用量、顶栏三控件（显示币种选择框/自动刷新+秒数/刷新）与 6 页签全部正常。回归：全量测试、smoke 通过。

v0.7.5：面板顶栏与计价覆盖卡改版。①顶栏显示设置收纳进齿轮弹层——用户否决「显示币种选择框/自动刷新/刷新按钮」直排顶栏的形态（太丑且挤占避让宿主四钮的空间），bar-tools 定型为 时间范围/刷新(图标)/设置齿轮/退出 四件；齿轮弹出「显示设置」浮层装 币种选择框（保留文字说明）与自动刷新开关+秒数（未勾选时秒数行置灰），Escape/点击外部关闭并归还焦点，窄屏 .tb-settings .pop 保持 right:0 贴右缘展开（.pop 的 left:0 窄屏覆盖只作用于 .range）；②**未计价模型清单（unpriced_models）整体移除**——v0.7.0 引入的计价覆盖体检清单先被否为太丑、改紧凑行加比例条后仍被否决，用户裁决直接去掉：/costs 不再返回 unpriced_models（UnpricedModel 类型与分组查询删除），计价覆盖卡只剩 请求总数/已计价请求/价格覆盖率/总费用 四项统计；教训已记 MEMORY（窄卡内塞列表/排行类组件的形态用户接受度差）。回归：全量测试、smoke 通过。

v0.7.6：**修复 Source 字段泄露上游 API Key**——用户导出的请求明细实锤错误行 source 列带完整上游 Key。根因已对照宿主源码定位：宿主 `resolveUsageSource` 在 auth 无邮箱/账号信息时回落把上游 api_key 原样填进 usage Record.Source，而插件 `RedactSource` 启发式（仅拦 `sk-` 前缀与 ≥32 位纯字母数字）拦不住带分隔符或较短的 Key，写入/读取两道清洗同时失效。四层修复：①`RedactSource` 强化——任一空白分隔 token 长度 ≥20、字符集仅 `[A-Za-z0-9._-]` 且同时含字母数字即判凭据整串清空（邮箱 @/URL / 被字符集排除正常保留；带数字的合法长标签会被误杀，属「宁可误杀」取舍，无分隔符短 Key 仍是盲区）；②store 写入收口——`insertRequestTx`（requests 唯一 INSERT 点）/`upsertRollupTx`/`fillKeeperZeroTx` 三处自带清洗，未来任何调用方不可绕过；③执行器 `buildRequest` 对 SourceFormat 清洗；④`GroupByDimension` 的 source 维度读侧清洗，历史脏 rollup 行随保留期老化（不做 DB 迁移）。测试钉：redact_test.go 新规则表、TestRedactSourceAtWrite、TestGroupByDimensionSourceRedacted。回归：全量测试、smoke、abi-smoke 通过。

v0.8.4：**修复上游无响应时按预占全额扣费（实测多扣 5 万倍）+ 预占估算改准**。用户导出 CSV 实锤：agentrouter 上游空响应（实际计费 0 输入/10 输出 token ≈ $0.00012）的请求因零用量走 missing_usage=settle_reserved 兜底，按「(body字节/2 + max_tokens) × 四档最贵档」的全额预占入账，22 万 token 上下文 + max_tokens=128000 的 agent 请求被扣 $7.05（audit 实锤 quota.reserve 与结算金额完全相等）。两层修复：①**预占估算改准**——输入估算 body 字节/2 → /3（英文/代码约 4 字节/token 高估 33%，CJK 最密 3 字节/token 恰好覆盖，base64 约 3 字节/token）；预占金额改分档计——输入估算按输入侧最贵档（input/cache_read/cache_creation 取最大）、输出估算按输出价，替代「总额 × 四档最高价」的系统性放大（同场景 7.05 → 2.46 USD）；ReservationRequest 新增 EstimatedInput/EstimatedOutput（任一非零走分档，全零兼容旧口径），main.go 三处直连/图片与 main_routes.go 路由路径全部携带拆分；routes.go BuildRouteEnv 的 input_tokens 变量同步 /3 口径（docs/routing.md、DESIGN.md §12.5 已更新）。②**Settle 新增 noResponse 第 5 参**——执行器判定「未产生任何响应数据」（HTTP≥400、空响应体、流式一条块都没收到即 firstChunkAt.IsZero()）时，零用量按**零成本结算**（不是 Release：请求行照常落库 cost 0 保留可观测性，预占全额退回）；有响应数据但缺 usage 仍按预占入账，settle_reserved 防逃逸语义保留；直连非流式（status≥400 或空体）、直连流读泵（firstChunkAt）、路由 failover 终局 HTTP 错误、pumpRoutedStream 六处 settle 点全部接线，连带修复路由拨号 HTTP 错误此前也被按预占全额扣费的问题；注意请求行 token 恒记真实值（零用量即 0），预占估算只进额度累计器。测试：新增 TestReserveTieredHeldCost（分档/旧口径兼容/缓存写档参与输入侧取贵）与 TestSettleNoResponseZeroCost（无响应零成本+请求行落库/有响应缺 usage 按分档预占入账/额度扣减对账）；TestEstimateTokens、TestTestRouteDryRun 估算断言更新为 /3。教训已记 MEMORY（兜底计费必须与真实成本可能性对齐）。回归：全量测试、smoke 通过。

v0.8.3：**美元/人民币分开入账，折算改实时汇率不再锁定 + README 完全重写**。①计价折算重写（用户明确要求「美元和人民币是分开的，汇率不要锁定」）：usdCost/costForRule 改为 Service 方法，CNY 规则的美元等值按**当前实时汇率**折算（svc.ExchangeRate：30 分钟自动刷新 + meta 缓存 + 双源兜底 7.20），不再读取规则上的 fx_rate_milli；原生入账不变（CNY 规则价格与 cost_native 恒人民币原值，与汇率无关）。fx_rate_milli 废弃为兼容列——normalizeCurrency 恒归一为中性值 1000（旧库存量值保留但重存后失效，免 DB 迁移），httpapi 保存路径删「未给汇率时取当前汇率锁定」。面板：计价表单删「锁定汇率」输入（币种选择保留并附实时口径说明），价格列/请求明细/详情/试算器的 CNY 提示全部改「按当前实时汇率折算」。测试改为与 ExchangeRate 实际值对账（测试机可能真连汇率源 6.71 也可能走兜底 7.20，不锁具体数字）。②README 完全重写（用户否决旧版「不符合项目实际且不美观」）：居中 logo+标题+一句话定位+5 枚 shields.io 徽章（发版/CI/MIT/Go/平台，flat-square）+页内快速导航开头；功能按四组重组（密钥额度/用量统计/模型路由/通知报告）；FAQ 改 details 折叠；修正两处不符实际——密钥明文可经详情「查看明文」解密回显（旧版写「无法找回」是错的，功能实为 POST /keys/reveal 用 pepper 解密 EncryptedMaterial，Cache-Control: no-store）、许可证表述改「本项目以 MIT 协议开源」；美元/人民币说明按新口径重写。③顺带（前两个未发版提交）：v0.8.2 后修复概览圆环图例长名字挤出占比列（.donut-legend-item 是 grid 子项 min-width:auto 不可收缩，加 min-width:0）；实时页签改版（进行中请求从系统页迁出单开页签、目标健康/进行中请求两面板 v0.8.0 误用不存在的 .tbl 类渲染成裸表格、ListHeldReservations 时间列直接扫 *time.Time 触发 500 的 Scan 报错修复）、表格脚注 .note.tbl-foot 统一。回归：全量测试、smoke、devserver+seed 浏览器实测（价格表单无汇率输入、CNY 规则保存/列表实时汇率 title、圆环长标签截断、七页签零 JS 错误）。

v0.8.2：**修复概览圆环图例长名字挤出占比列**。模型占比/密钥消耗的图例行（.donut-legend-item，button）是 .donut-legend（grid）的子项，grid 子项默认 min-width:auto 不可收缩——长模型名/密钥标签把整行撑出轨道（实测 172px 轨道被撑到 410px），名字不截断反而整行溢出，占比列被推出图例区不可见；.dl-name 自身的 min-width:0+ellipsis 管不到这层（收缩要先从 grid 子项放开）。修复：给 .donut-legend-item 加 min-width:0，名字恢复省略号截断（title 悬浮可见全名），占比列回到容器内；1280/400px 两档视口实测。教训已记 MEMORY（flex/grid 嵌套收缩链：内层 min-width:0 无效时先查外层容器子项的 min-width:auto）。

v0.8.1：**实时页签改版 + 目标健康样式修复 + 在途视图 Scan 报错修复**（v0.8.0 新增两个面板的跟进修复）。①`ListHeldReservations` Scan 报错修复——reservations 时间列自建表起就存 UnixMilli 整数（所有既有查询经 scanReservation 按 int64 扫再转换），v0.8.0 新写的 ListHeldReservations 唯一直接扫 *time.Time，面板「进行中请求」加载即 500（"unsupported Scan, storing driver.Value type int64 into type *time.Time"）；改为与 scanReservation 同口径 int64+time.UnixMilli，新增 TestListHeldReservationsScansMillisTimestamps 钉住。②**两个面板样式全错的根因**：CSS 里根本没有 `.tbl` 类（全仓表格样式只有 `table.data`），v0.8.0 新增「进行中请求/目标健康」时用了不存在的类名，渲染成无分隔线/无对齐/无内边距的裸表格——两处均改 `table.data` + w-grow/cell-mono/cell-clip 既有类族。③「进行中请求」从系统页迁出**单开「实时」页签**（tabs 第 6 项， loaders.live）：表格加「最近心跳」列（rel() 相对时间），头部统计「共 N 条在途 · M 条心跳超时」；本页专属 5s 自动刷新开关（held-auto 偏好存 localStorage；不走顶栏全局自动刷新——那个间隔可到 86400s 对实时视图太迟钝），页面隐藏/切走页签时暂停轮询；密钥标签走 /keys/candidates 全量候选（keyCandidates 缓存，keyLabelOf 先查当前页再查跨页缓存——keysView.cache 只有当前分页页）。④「目标健康」重写：排序（冷却中 > 失败数降序 > 请求量降序）、空态文案区分「无启用集合」与「规则未引用目标」。⑤seed.go 补两类种子：在途预占（2 活跃 + 1 心跳超时，给实时页签供数据）与模型集合（smart/cheap 启用 + draft-off 停用，给目标健康供数据）——注意规则语言目标名必须带引号（`-> "model"`）、逻辑运算符是 `&&`/`||` 不是 and/or，裸名/英文连词会编译失败导致 refs 为空、目标健康面板不显示该集合（编译失败优雅降级不报错，面板只见其他集合）。⑥表格脚注样式收尾：直接裸放在 table-wrap 后的说明行（无 panel-body 包裹）此前一处内联样式底部贴边、一处无内边距；统一 `.note.tbl-foot`（padding 与 .pager 同口径 10px 16px + 顶部 1px 分隔线），目标健康/进行中请求两处换类。回归：全量测试、smoke、devserver+seed 浏览器实测（七页签遍历零 JS 错误、表格样式 computed style 断言、5s 自动刷新实测生效、系统页确认无残留面板）。

v0.8.0：**双写判重根因修复 + 汇率自动刷新 + CI/配置批 + 可观测性功能批（schema v14）**。①重复入库根因修复（用户实锤「挂着不动请求数自己变少」，已对照宿主源码核实）：宿主 usage 回调走单 worker 后台队列（积压时晚于请求完成超 8s 认领宽限即 miss）+ 宿主 Latency 含全链路开销（与执行器纯上游延迟差可达数秒，旧谓词「延迟±150ms」必然漏判）→ 被动行残留重复、由对账删掉时请求数下降；duplicateProbeTx/dedupePairSQL 重写为结构化区间谓词：被动行 ts 落在执行器请求区间内 + 完成时刻对齐（±10s+30s 队列尾延迟）+ token 相容过滤（差 ≤64，同模型并发下区分不同请求的关键信号）；PassiveDedupeHint/FindDuplicateExecutor 签名加 token 计数。②汇率自动刷新：fxRefreshLoop（30min+启动即刷，仅租约持有者，fxStop 收口），摆脱纯懒加载。③CI 工作流 ci.yml（gofmt/vet/node --check/test）+ gofmt 全清 + backup.max_bytes 可配置（默认 256MiB，旧 64MiB 硬上限高频部署必炸）。④失败原因留痕（schema v14：requests 加 status_code/error_note，SanitizeErrorNote 清洗截断；路由 failover 轨迹与 ai_judge 回落错误并入结算行；route.failover/route.ai_fallback 审计退役；audit_retention_days 默认 90 接入 ApplyRetention；明细「原因」低频列 + CSV 两列）。⑤可观测性批：GET /reservations/held（进行中请求视图+系统页面板）、GET /model-routes/health（目标健康：冷却+近 60 分钟失败，RoutesHealth 走 ListRoutesCompiled 取 Refs）、错误率告警（全局滑动窗口+边沿触发，状态行 __global__）、密钥临期预警（ExpireWarnDays 四舍五入天数）、/health 加 wal_bytes、报告环比。⑥计价试算器（纯前端 openSheet，BigInt 与 costForRule 同口径；教训：console.js 单 IIFE，追加代码必须在「启动」段之前，否则监听器绑定 ReferenceError 被吞、点击无反应）。⑦README 重写（快速上手给最小可粘贴配置、FAQ、升级兼容说明、配置参考补齐全部新字段）。回归：全量测试、smoke、ABI 符号导出验证、devserver+seed+浏览器实测全部页签与新面板/弹窗交互。浏览器实测环境坑已记 MEMORY：devserver 是 go run，改前端后必须杀 18080 进程重启；浏览器 reload 吃 HTML 缓存需 ?v=N 强制拉新。

**发版流程变更（v0.6.2 起）**：代码提交与发版提交分离——功能/修复提交不带版本号；发版单独一个 `release: vX.Y.Z` 提交（registry.json + AGENTS.md 版本历史 + 本流程规则），再打标签推送。v0.7.0：**修复批（10 项正确性/健壮性）+ 性能批 + 面板功能批**。修复：①ai_judge 失败回落兜底链被 resolveRouting 拒绝——ResolveChain 在 AI 失败时返回（兜底链, true, AI错误），链与错误非互斥，main 层 `cerr != nil` 一律拒绝违背 DESIGN §12.2「请求不受阻」语义，改为 `cerr != nil && chain == nil` 才拒绝（TestResolveRoutingAIFallback 钉住）；②C ABI 入口补 panic 防护（safeDispatch recover→plugin_error 信封）——c-shared 库里 Go panic 跨 C 边界是 abort，插件内任何 panic 都会击穿宿主进程；③重置统计补清 4 个 token 累计器（原只清金额，金额归零 token 仍在，配 Token 限额的 Key 被旧用量占用误拒）；④RestoreFrom 快照表补 model_routes/notify_endpoints/report_configs 三张表，恢复后触发 notifyPricingChanged/notifyRoutesChanged 失效服务层内存快照；⑤趋势图周粒度修正——SQLite `weekday 1` 修饰符在日期已是周一时不前进，旧口径把周一的桶退到上一周，改为先 `-6 days` 再 `weekday 1`（TestTrendsWeekBucketMonday）；⑥usageClaim 补 released 标志（c.mu 内先置位再摘桶，attach/openFor 同锁互斥）修「选择命中→交付」窗口里执行器放弃认领导致宿主用量凭空丢失的竞态，结算失败且口径已 attach 的极端情况丢弃并留日志；⑦reconfigure 失败路径清空运行态（svc=nil 走干净的 service_unavailable，不再对已关闭库报错静默丢被动记录）并停旧通知循环，新增 Service.Close 停 reservationBeatLoop 修每次 reconfigure 泄漏一个心跳 goroutine；⑧FindKeyByCallerScope 兜底鉴权补 Usable 过滤（enabled/未撤销/未过期），防已撤销 Key 因排序最新被采信；⑨流式读泵（直连+路由两处）加 10 分钟无进度守护 startStreamIdleWatchdog——宿主回调是 C ABI 同步调用无取消语义，stream_read 挂死时主动 closeHostModelStream 解除阻塞，防在途预占被心跳无限续命、stale 清扫永远扫不到。性能：系统页 7 个 COUNT 合并单次往返；ApplyRetention 改每批独立写事务分批删除（20000 行/批；usage_rollups 是 WITHOUT ROWID 表，用复合主键元组 IN 子查询）；HoldReservation 免提交后回读按入参组装返回；include_usage 注入改 json.Decoder 定位顶层 stream_options 字节区间+字节级拼接——直连流式路径不再对每请求整包 Unmarshal+Marshal（Marshal 会重排序键并 HTML 转义 <>&，属隐性请求体变异），除注入点外原文体逐字节保留（TestRequestBodyWithStreamUsage 七分支）。功能：**导出接通**——趋势面板新增「导出」弹窗（图表 PNG 服务端渲染/数据 CSV 二选一），维度聚合新增导出 CSV，请求导出上限 5 万→10 万，导出文件名统一带时间范围标记（_YYYYMMDD-HHMM 或 _all）；实锤并修复导出响应头时序缺陷——「先写体再设头」在真实 HTTP 下 Content-Type/Content-Disposition 被首写快照全部丢弃，新增 svc.ExportTarget/exportFileName 让处理器先预置头再写体；**计价覆盖体检**——/costs 新增 unpriced_models（有流量但 priced=0 的模型 Top 10），概览计价覆盖卡内嵌清单、点击跳转该模型预筛的请求明细；**额度耗尽预测**——密钥详情弹窗按今日消耗/今日已过比例（UTC）推算日速率给出触顶预估，档位选取与 usdPick/tokPick 同口径，今日无消耗或已过 <5% 不外推；**偏好跨设备同步**——列显示/每页条数/时间范围/概览指标经 savePref 双写 localStorage 与 /preferences 的 ui_ 前缀键，登录后 syncPrefsFromServer 以服务器为准回灌本地并整页重载一次（sessionStorage 防循环；custom 范围无起止不同步）。**决策：审计功能整体弃用（2026-08 用户明确指示）**——不再新增审计向功能（审计页/审计导出/归档一律不做），audit_events 仅维持内部留痕。

## 这是什么

面向 CLIProxyAPI 的 **Go `c-shared` 插件**，插件 ID：`cpa-usage-manager`（宿主按动态库文件名派生 ID）。定位：插件 Key（`cum-...`）额度管理为核心 + 单一管理面板展示用量/额度/审计。

- Go module：`github.com/drowsylazy/cpa-usage-manager`
- Go 1.26+，`CGO_ENABLED=1` 才能产出 `.so`/`.dll`/`.dylib`
- 这是**重写**，不是合并：不搬运两插件源码，上游仓库只作实现参考与测试基准
  - 参考源：`AITNR/cap-token-usage-tracker`（统计）、`yuluo688/credit-manager`（Key 额度）

## 已锁定的设计决策（不得违反）

- **存储**：单一 SQLite（`modernc.org/sqlite`，纯 Go），不用 bbolt；单写者 + WAL + 跨进程锁 + handover 租约
- **Key 格式**：`cum-<kid>-<secret>`（不是 `tk-`）；明文仅签发时返回一次，库中只存 HMAC 哈希 + AES-GCM 密文 + pepper
- **金额**：整数 micro-USD，无浮点；只对 Input/Output/Cache Read/Cache Creation 计价，各类别向上取整后相加
- **默认行为**：`quota.enabled=true` 接管前端鉴权；置 `false` 退回纯统计（被动 usage 记录）
- **usage 写入**：单一路径一次入库（逐请求记录 + 分钟聚合 + 账本 + 审计），无双写
- **无公开/自助页面、无会话令牌**：唯一读取面是 `/console` 管理面板，全部数据经宿主管理密钥鉴权的 `/v0/management/plugins/cpa-usage-manager/*`；HTML 壳不含数据；不保留 tracker 的「普通/完整模式」双前端与 `X-Full-Mode-Session`
- **不存上游 API Key 密文**：认证字段只保存清洗后的展示信息，不提供上游 Key 明文回显/标签
- 计价统一为一张 `pricing_rules` 表（match_kind exact/glob/regexp + priority），同时服务额度结算与面板费用展示

## 架构分层

`internal/{config, service, store, money, usageparse, fx, httpapi, web}`，入口为 `main.go`（内联 C ABI，无 cgo 构建标签）。

## 约定

- 推送后**不**监控 GitHub Actions workflow 与发布资产，由维护者自行确认
- 仓库文档与用户沟通使用**中文**。
- 空数据库默认自动建 `default` caller、全模型免费计价规则（`unknown_policy: allow`），但不自动签发任何 Key
- pepper 只在环境变量或 `data_dir/key-peppers`（0600），绝不入库/入日志/入 API 响应
- `data_dir`（0700）备份时须连同 `key-peppers` 一起备份
- 构建产物名必须是 `cpa-usage-manager.{so,dll,dylib}`；本机替换 DLL 后须重启宿主
- 敏感接口（签发/解密/备份/恢复/重置）响应须 `Cache-Control: no-store`
- 面板改动请用拟真数据实测，不要凭空调参：`go run scripts/seed.go` 注入种子（写默认 data_dir，可用 `CPA_DEV_DATA_DIR` 改；固定随机种子、可重复运行），再 `go run scripts/devserver.go` 开 <http://127.0.0.1:18080/console>（管理密钥 `dev-secret`）。`scripts/seed.go` 与 `scripts/devserver.go` 都是 `//go:build ignore` 的开发脚本，不参与构建
- 改图表系列色（`--series-1..4`）必须重跑配色校验（明度带 / 彩度下限 / 相邻 CVD ΔE≥8 / 常视觉 ΔE≥15 / 对比≥3:1），浅深两模式都要过；不要把图表系列色换回品牌语义色（实测两模式均不达标，见项目状态）